package org.example;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class MainTest {
  /*  //GENAU 8 Chars
    @Test
    void checkPasswordLength_ShouldReturnStringTollesPasswort_perfectAmountOfChars() {
        //GIVEN
        String expected = "Tolles Passwort!";
        //WHEN
        String actualy;
        actualy = Main.checkPasswordLength("t2312454");
        //THEN
        Assertions.assertEquals(expected, actualy);
    }

    // zu wenig Zeichen
    @Test
    void checkPasswordLength_ShouldReturnStringNotEnoughChars_LessThen8Chars() {
        //GIVEN
        String expected = "Password muss min. 8 Zeichen enthalten!";
        //WHEN
        String actualy;
        actualy = Main.checkPasswordLength("t231245");
        //THEN
        Assertions.assertEquals(expected, actualy);
    }

    // Mehr als 8 Chars
    @Test
    void checkPasswordLength_ShouldReturnStringEnoughChars_MoreThen8Chars() {
        //GIVEN
        String expected = "Password muss min. 8 Zeichen enthalten!";
        //WHEN
        String actualy;
        actualy = Main.checkPasswordLength("t231245");
        //THEN
        Assertions.assertEquals(expected, actualy);
    }

    @Test
    void checkIfPasswordContainsDigits_ShouldReturnExpected_WithDigits() {
        //GIVEN
        String expected = "Tolles Passwort!";
        //WHEN
        String actualy;
        actualy = Main.checkIfPasswordContainsDigits("password2");
        //THEN
        Assertions.assertEquals(expected, actualy);
    }
    @Test
    void checkIfPasswordContainsUpperAndLowerCase_ShouldReturnExpected_WithUpperAndLower() {
        //GIVEN
        String expected = "Tolles Passwort!";
        //WHEN
        String actualy;
        actualy = Main.checkIfPasswordContainsUpperAndLowerCase("passwordS2");
        //THEN
        Assertions.assertEquals(expected, actualy);
 */
}
